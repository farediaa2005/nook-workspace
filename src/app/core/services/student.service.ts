import { Injectable } from '@angular/core';

/**
 * Service for standalone student management and integrations.
 */
@Injectable({
  providedIn: 'root'
})
export class StudentService {
  // Student core service methods can be added here as needed
}
