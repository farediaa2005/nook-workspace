/**
 * Backward-compatibility re-export file for Classroom models and DTOs.
 * Canonical definitions are now located in ./classroom.model.ts (Clean Architecture Layer 1).
 */

export * from './classroom.model';
