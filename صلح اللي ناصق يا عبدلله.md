# النواقص والمقترحات المطلوبة في الـ Backend API الخاصة بموديول القاعات (Classroom & Reservations)

> **موجه إلى:** عبدالله (Backend Development Team)  
> **الموضوع:** الحقول ونقاط النهاية (Endpoints) المطلوبة لتشغيل موديول القاعات (Classroom) والتقويم والورديات بالكامل وبأعلى دقة مالية وتدقيقية.  
> **تاريخ التحديث:** سبتمبر 2026

---

## 1. تضمين بيانات المحاضر والقاعة والكاترينج في قائمة الجلسات (`GET /api/Classrooms`)

### الوضع الحالي:
الـ Endpoint يرجع حقول الـ Entity المباشرة فقط (`id`, `roomId`, `instructorId`, `timeFrom`, `timeTo`, `reservationCost`, `status`) بدون بيانات العلاقات.

### المشكلة:
1. عدم وجود **اسم القاعة (`roomName`)**، مما يجبر الواجهة على عمل Join محلي للغرف.
2. عدم وجود **اسم المحاضر (`instructorName`)** و**رقم هاتفه (`instructorPhoneNumber`)**، مع أنهما موجودان في استدعاء التفاصيل `GET /api/Classrooms/{id}`.
3. عدم وجود **إجمالي طلبات الكاترينج (`cateringTotal`)** للجلسة، مما يجبر الواجهة على تنفيذ استدعاء منفصل لكل قاعة على حدة (`GET /api/classrooms/{classroomId}/catering`) مسبباً مشكلة $N+1$ Requests.

### المطلوب إضافته في الـ Response DTO (`ClassroomDto`):
```json
{
  "id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
  "roomName": "قاعة ورش العمل (Workshop Hall)",
  "instructorName": "د. أحمد حسام",
  "instructorPhoneNumber": "01012345678",
  "cateringTotal": 85.00,
  "hourlyRate": 60.00,
  "totalCost": 205.00,
  "status": 1
}
```

---

## 2. تسجيل وإنشاء حجز جديد (`POST /api/Classrooms`)

### الوضع الحالي:
الـ Request يتطلب إرسال `instructorId` كـ GUID إلزامي مسجل مسبقاً.

### المشكلة:
في صالة الاستقبال السريعة، قد يدخل محاضر أو عميل جديد لحجز قاعة، ويحتاج موظف الاستقبال لتسجيل اسمه ورقمه فوراً دون الحاجة لمغادرة الشاشة والذهاب لشاشة "إضافة محاضر" وإنشائه أولاً ثم العودة.

### المطلوب دعمه في الـ Request Body (`CreateClassroomDto`):
دعم تمرير بيانات المحاضر مباشرة إذا لم يكن مسجلاً مسبقاً:
```json
{
  "roomId": "guid-room-id",
  "instructorId": "guid-or-null",
  "instructorName": "المهندس شريف طارق",
  "instructorPhoneNumber": "01123456789",
  "title": "ورشة عمل تطوير واجهات المستخدم",
  "activity": "ورشة عمل تطوير واجهات المستخدم",
  "timeFrom": "2026-09-09T10:00:00Z",
  "timeTo": "2026-09-09T12:00:00Z",
  "expectedAttendees": 15,
  "hourlyRate": 75.00,
  "printing": 10.00
}
```
* **سلوك الباك إند المقترح:** إذا كان `instructorId` موجوداً يتم الربط به، وإذا كان غير موجود وتم إرسال `instructorName` و `instructorPhoneNumber` يقوم الباك إند بالبحث عنه أو إنشائه تلقائياً في جدول `Instructors` وربطه بالجلسة.

---

## 3. تسوية الخروج والتسجيل المالي وحفظ حالة الانتهاء (`PUT /api/Classrooms/{id}/checkout`)

### الوضع الحالي والمشكلة:
1. الـ Endpoint الحالي عند استدعائه يقوم بتسجيل `timeTo` في قاعدة البيانات ولكن **لا يقوم دائماً بتحديث وتخزين `Status = 2` (Completed)**، مما يتسبب في عودة القاعة للظهور كأنها لا تزال نشطة إذا كان وقت الحجز سارياً!
2. لا يدعم تسجيل هوية الوردية والمسؤول عن الشيك أوت:
   - قد يقوم الموظف (أ) بفتح الحجز في الشيفت الصباحي.
   - ويقوم الموظف (ب) بعملية الشيك أوت واستلام النقدية في الشيفت المسائي.
   - الباك إند حالياً لا يسجل `checkoutShiftId` أو `checkoutStaffId`، مما يصعب تدقيق صندوق الكاشير بدقة.

### المطلوب في الـ Request والـ Logic:
1. **تحديث صريح لحالة القاعة:**
   - عند نجاح الشيك أوت، يجب تحديث `Status = 2` (Completed / Left) في جدول `Classrooms`.
2. **استقبال بيانات التدقيق المالي والوردية:**
```json
{
  "timeTo": "2026-09-09T14:30:00Z",
  "actualAttendees": 12,
  "paymentMethod": "Cash",
  "payWay": 1,
  "usePackageHours": 2.0,
  "packageId": "guid-package-id-or-null",
  "paidAmount": 250.00,
  "reservationCost": 200.00,
  "cateringAmount": 50.00,
  "printing": 0.00,
  "discount": 0.00,
  "shiftId": "guid-current-active-shift",
  "staffId": "guid-staff-profile-performing-checkout",
  "note": "تم سداد الحساب كاش واستلام الإيصال"
}
```

---

## 4. قائمة الحجوزات المجدولة في التقويم (`GET /api/Reservations`)

### الوضع الحالي:
الـ Endpoint يرجع حقول `roomId` و `instructorId` فقط كـ GUIDs مجردة.

### المطلوب إضافته في الـ Response DTO (`ReservationDto`):
```json
{
  "id": "guid-reservation-id",
  "roomId": "guid-room-id",
  "roomName": "قاعة أ (Hall A)",
  "instructorId": "guid-instructor-id",
  "instructorName": "د. حازم إسماعيل",
  "instructorPhoneNumber": "01234567890",
  "activity": "دورة لغة ألمانية مكثفة",
  "timeFrom": "2026-09-09T09:00:00Z",
  "timeTo": "2026-09-09T11:00:00Z",
  "dateFrom": "2026-09-09T00:00:00Z",
  "dateTo": "2026-09-09T00:00:00Z",
  "reservationCost": 160.00,
  "status": 1
}
```

---

## 5. ميزة تجزئة الحجز الذرية (Split Scheduled Reservation)

### المشكلة:
في التقويم الإداري لقاعات التدريب، يحتاج الأدمن أحياناً لاقتطاع ساعة استراحة أو تجزئة حجز طويل (مثلاً من 10:00 ص إلى 4:00 م) لتأجير ساعة فارغة في المنتصف لعميل آخر.  
الفرونت إند حالياً يضطر لحذف الحجز القديم وإنشاء حجزين جديدين، وهو ما قد يفشل جزئياً إذا انقطع الاتصال في المنتصف.

### الـ Endpoint المقترح:
* **المسار:** `POST /api/Reservations/{id}/split`
* **Request Body:**
```json
{
  "cutStartTime": "13:00",
  "cutEndTime": "14:00"
}
```
* **السلوك المطلوب في الباك إند:**
  تقسيم الحجز في معاملة ذرية واحدة (Database Transaction) إلى حجزين مع إعادة حساب التكلفة لكل منهما بناءً على ساعات كل فترة وسعر ساعة القاعة.

---

## 6. ملخص الصلاحيات والأمان (Authorization & Ownership)

- يجب أن تسمح عمليات الحجز والتشيك أوت لأي موظف استقبال مرخص (`Staff` / `Admin`) مسجل ولديه شيفت مفتوح بالتعامل مع أي قاعة حتى لو كان من فتحها موظف آخر في شيفت سابق، مع تسجيل هوية كل موظف أمام خطوته (`CreatedByStaffId` و `CheckedOutByStaffId`).
