# طلبات جديدة من الباك (Backend Requirements & Gap Analysis)

> **المشروع:** نظام Nook Workspace Management  
> **الموجّه إلى:** فريق تطوير الـ Backend (Backend Development Team - م/ عبدالله)  
> **المعدّ:** Senior Full-Stack Engineer + Software Architect  
> **التاريخ:** سبتمبر 2026  
> **حالة الوثيقة:** رسمية ونهائية - تشمل مراجعة شاملة لملف الـ `openapi.json` ومطابقته مع متطلبات نظام التشغيل والإدارة والـ UX.

---

## 📑 فهرس المحتويات
1. [الملخص التنفيذي وموقف النظام](#1-الملخص-التنفيذي-وموقف-النظام)
2. [الفجوة 1: مركز الإشعارات الموحد للموظفين والإدارة (Global Staff Notification Center)](#فجوة-1-مركز-الإشعارات-الموحد-للموظفين-والإدارة-global-staff-notification-center)
3. [الفجوة 2: نظام سجل التدقيق الشامل وتتبع العمليات (Audit Trail / Who Did What)](#فجوة-2-نظام-سجل-التدقيق-الشامل-وتتبع-العمليات-audit-trail--who-did-what)
4. [الفجوة 3: ملف وتحليلات الطالب المجمعة (Student Profile & Comprehensive Analytics)](#فجوة-3-ملف-وتحليلات-الطالب-المجمعة-student-profile--comprehensive-analytics)
5. [الفجوة 4: محرك قواعد الخصومات وحماية الاستخدام (Discount Rule Engine & Student Protection)](#فجوة-4-محرك-قواعد-الخصومات-وحماية-الاستخدام-discount-rule-engine--student-protection)
6. [الفجوة 5: إعدادات وحدود التنبيهات التشغيلية (Operational Alerts & Thresholds Settings)](#فجوة-5-إعدادات-وحدود-التنبيهات-التشغيلية-operational-alerts--thresholds-settings)
7. [الفجوة 6: بيانات الفواتير الرسمية لجلسات القاعات والورك سبيس (Official Invoicing API)](#فجوة-6-بيانات-الفواتير-الرسمية-لجلسات-القاعات-والورك-سبيس-official-invoicing-api)
8. [الفجوة 7: إثراء استجابة قائمة القاعات النشطة لتفادي N+1 Queries](#فجوة-7-إثراء-استجابة-قائمة-القاعات-النشطة-لتفادي-n1-queries)
9. [مصفوفة الأولويات وخطة التسليم](#مصفوفة-الأولويات-وخطة-التسليم)

---

## 1. الملخص التنفيذي وموقف النظام

بعد فحص الـ `openapi.json` الحالي ومقارنته بما يحتاجه نظام Nook التشغيلي اليومي، تبيّن الآتي:
- **ما تم إنجازه بنجاح في الباك إند:** ورديات العمل (`GET /api/Shifts/current` و `POST /api/Shifts/{id}/recalculate` وقيد الوردية الواحدة)، وحجوزات القاعات المتكررة (`POST /api/Reservations/check-conflict` و `POST /api/Reservations/{id}/cancel-day` و `POST /api/Reservations/{id}/create-classroom`).
- **الفجوات الجوهرية المتبقية:**
  1. لا يوجد أي Endpoint لإشعارات الموظفين والإدارة (`/api/Notifications`)؛ الإشعارات الحالية محصورة فقط بتطبيقات الموبايل (`/api/mobile/student/notifications` و `/api/mobile/instructor/notifications`).
  2. لا يوجد نظام Audit Trail إطلاقاً في الباك إند لتتبع من قام بفتح جلسة، إلغاء حجز، تطبيق خصم، تعديل حساب أو تغيير وردية.
  3. لا يوجد Endpoint يوفر تحليلات الطالب وسجل الساعات والمساحات السابقة، مما يضطر الفرونت إند لحسابها يدوياً من عدة استدعاءات متفرقة.
  4. نظام الخصومات الحالي يعتمد على الكوبونات اليدوية والخصومات الثابتة فقط، ولا يحتوي على محرك قواعد تلقائي (Rule Engine) يمنح الخصم عند وصول الطالب لساعات معينة (مثل 100 ساعة أو 200 ساعة) ويحمي من تكرار استخدام الخصم قبل تحقيق الهدف التالي.

---

## فجوة 1: مركز الإشعارات الموحد للموظفين والإدارة (Global Staff Notification Center)

### Feature
مركز الإشعارات المباشر لإدارة عمليات الصالة والقاعات والمنتجات وتنبيهات الإقامة الطويلة في واجهة الويب (`Global Notification Center`).

### Current Backend Support
يقتصر دعم الإشعارات حالياً على تطبيقي الموبايل الخاصين بالطالب والمحاضر:
- `GET /api/mobile/student/notifications`
- `PUT /api/mobile/student/notifications/{id}/read`
- `GET /api/mobile/instructor/notifications`
ولا توجد أي نقاط نهاية (Endpoints) لإشعارات موظفي الاستقبال أو الكاشير أو المشرفين.

### Missing
نقاط نهاية موحدة للموظفين والإدارة لعرض، قراءة، وحذف الإشعارات التشغيلية، مع دعم Real-Time (SignalR Hub).

### Required API & Endpoint Proposals
1. `GET /api/Notifications`: جلب إشعارات الموظفين الحالية مع خيارات الفلترة.
2. `PUT /api/Notifications/{id}/read`: تعليم إشعار كمقروء.
3. `PUT /api/Notifications/read-all`: تعليم كافة الإشعارات كمقروءة.
4. `DELETE /api/Notifications/{id}`: إخفاء/حذف إشعار.
5. `SignalR Hub`: `/hubs/notifications` لإرسال الأحداث اللحظية `ReceiveNotification`.

### HTTP Methods
- `GET`, `PUT`, `DELETE`

### Request DTO (`GetNotificationsQuery`):
```json
{
  "unreadOnly": false,
  "type": "Workspace", // Workspace | Classroom | Package | SharedRoom | SilentRoom | Catering | LongStay | Shift
  "severity": "Warning", // Info | Warning | Critical | Success
  "pageNumber": 1,
  "pageSize": 30
}
```

### Response DTO (`PagedResult<StaffNotificationDto>`):
```json
{
  "items": [
    {
      "id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
      "type": "Catering",
      "title": "تنبيه انخفاض المخزون",
      "message": "المنتج 'قهوة أمريكانو' وصل رصيده إلى 4 قطع فقط (أقل من الحد الحرج 5).",
      "severity": "Critical",
      "createdAt": "2026-09-14T14:30:00Z",
      "relatedEntityType": "Product",
      "relatedEntityId": "7cb45d61-3312-4211-a8bb-32115166f001",
      "actionUrl": "/catering/show-products",
      "isRead": false,
      "readAt": null
    }
  ],
  "totalCount": 12,
  "unreadCount": 3,
  "pageNumber": 1,
  "pageSize": 30
}
```

### Required Fields
`id`, `type`, `title`, `message`, `severity`, `createdAt`, `relatedEntityType`, `relatedEntityId`, `isRead`.

### Business Logic
يقوم الباك إند آلياً بإنشاء إشعارات في الحالات التالية:
1. **Workspace:** اقتراب انتهاء وقت الجلسة (باقي ساعة، باقي 10 دقائق)، تجاوز الوقت المسموح، انتهاء الباقة.
2. **Classroom:** بدء حجز قاعة خلال ساعة، بدء حجز خلال 10 دقائق، انتهاء وقت الجلسة الفعلي، تجاوز الوقت.
3. **Catering:** انخفاض مخزون أي منتج عن الحد التحذيري (10) أو الحرج (5).
4. **Long Stay:** بقاء طالب أو جلسة لأكثر من 18 ساعة مستمرة.
5. **Shifts:** تذكير بفتح أو إغلاق الوردية.

### Authorization / Roles
- الأدوار المصرح لها: `SuperAdmin`, `Admin`, `Manager`, `Staff`.

### Validation
- التحقق من وجود المعرف `id` عند التعديل/الحذف.

### Dependencies
- خدمة الجدولة الخلفية Background Job (Hangfire أو Quartz.NET) لفحص الجلسات والمخزون دورياً وتوليد التنبيهات.

### Priority
🔥 **حرجة وعالية جداً (P1)**.

---

## فجوة 2: نظام سجل التدقيق الشامل وتتبع العمليات (Audit Trail / Who Did What)

### Feature
سجل تدقيق شامل وموحد لكل العمليات الحساسة في النظام لمعرفة: من فعل ماذا، متى، وعلى أي عنصر، والقيم قبل وبعد التعديل.

### Current Backend Support
غير موجود نهائياً في `openapi.json`. لا توجد أي جداول أو Endpoints معلنة للـ Audit Logs.

### Missing
- جدول وواجهة برمجية لاستعلام سجلات التدقيق مع إمكانية الفلترة بالكيان، المستخدم، والفترة الزمنية.

### Required API & Endpoint Proposals
1. `GET /api/AuditLogs`: استعلام سجل التدقيق العام.
2. `GET /api/AuditLogs/entity/{entityType}/{entityId}`: استعلام تاريخ وتدقيق عنصر معين (مثل تاريخ قاعة معينة أو طالب معين).
3. `POST /api/AuditLogs`: تسجيل حدث يدوي من الفرونت إند في حال كانت هناك عمليات واجهة تتطلب توثيقاً صريحاً.

### HTTP Methods
- `GET`, `POST`

### Request Query Parameters:
- `entityType`: `Classroom` | `Workspace` | `Student` | `Package` | `Discount` | `Catering` | `Shift` | `Payment`
- `entityId`: `Guid`
- `actorId`: `Guid` (المستخدم الذي نفذ العملية)
- `action`: `Created` | `Modified` | `Deleted` | `CheckedIn` | `CheckedOut` | `DiscountApplied` | `PaymentReceived` | `ShiftClosed`
- `from`: `DateTime`
- `to`: `DateTime`

### Response DTO (`AuditLogDto`):
```json
{
  "id": "e0d37e50-9304-4e2a-a9a8-e16548777123",
  "entityType": "Classroom",
  "entityId": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
  "action": "CheckedOut",
  "actorId": "1b9d6bcd-bbfd-4b2d-9b5d-ab8dfbbd4bed",
  "actorName": "أحمد مصطفى (كاشير الوردية الأولى)",
  "actorRole": "Staff",
  "timestamp": "2026-09-14T14:15:00Z",
  "ipAddress": "192.168.1.45",
  "summary": "تم إنهاء حجز قاعة ورش العمل واستلام مبلغ 350.00 ج.م نقداً",
  "oldValues": "{\"status\": 1, \"finalAmount\": null}",
  "newValues": "{\"status\": 2, \"finalAmount\": 350.00, \"payWay\": 1}",
  "metadata": "{\"shiftId\": \"2c8e31a1-3321-4d11-b9cd-99114422aa01\"}"
}
```

### Business Logic
- يقوم الـ EF Core Interceptor أو الـ MediatR Pipeline تلقائياً باعتراض عمليات `SaveChanges` وتسجيل التعديلات على الجداول الحساسة.
- يتم ربط كل حركة بالمستخدم الحالي الموثق بالـ JWT Token (`sub` / `nameid`) ورقم الوردية النشطة.

### Authorization / Roles
- مسموح بالقراءة لـ: `SuperAdmin`, `Admin`, `Manager`.
- غير مسموح بالمسح أو التعديل لأي دور منعاً للتلاعب بالسجلات (Immutable Logs).

### Priority
🔥 **حرجة ومحورية للأمان والمساءلة المالية (P1)**.

---

## فجوة 3: ملف وتحليلات الطالب المجمعة (Student Profile & Comprehensive Analytics)

### Feature
الملف الشخصي المتكامل للطالب (`Student Profile Drawer / Modal`) مع لوحة تحليلات فورية لاستخدام المساحات والساعات السابقة والحالية.

### Current Backend Support
- `GET /api/Students/{id}`: يرجع البيانات الأساسية فقط (الاسم، الكلية، الهاتف، الرصيد).
- `GET /api/Wallet/student/{studentId}/balance`: رصيد المحفظة.
- `GET /api/Packages/student/{studentId}`: باقات الطالب.
- لكن لا يوجد أي Endpoint لحساب عدد الزيارات أو توزيع الساعات بين القاعات والورك سبيس والغرف الصامتة.

### Missing
- استدعاء واحد يجمع إحصائيات الطالب وسجل الحجوزات لتفادي بطء التحميل وعمليات الحساب المرهقة في المتصفح.

### Required API & Endpoint Proposal
- `GET /api/Students/{id}/analytics`

### Response DTO (`StudentAnalyticsDto`):
```json
{
  "studentId": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
  "studentName": "كريم عبد العزيز",
  "college": "كلية الهندسة",
  "phone": "01099887766",
  "isBlacklisted": false,
  "walletBalance": 120.50,
  "visits": {
    "totalVisits": 48,
    "lastMonthVisits": 14,
    "currentMonthVisits": 8
  },
  "hours": {
    "totalHours": 182.5,
    "lastMonthHours": 52.0,
    "currentMonthHours": 31.5
  },
  "spaceUsage": [
    {
      "spaceType": "Workspace",
      "visits": 35,
      "hours": 120.0,
      "lastVisit": "2026-09-13T18:00:00Z"
    },
    {
      "spaceType": "Classroom",
      "visits": 8,
      "hours": 38.5,
      "lastVisit": "2026-09-10T14:00:00Z"
    },
    {
      "spaceType": "SilentRoom",
      "visits": 5,
      "hours": 24.0,
      "lastVisit": "2026-09-02T19:00:00Z"
    }
  ],
  "bookingHistorySummary": {
    "totalBookings": 12,
    "completedBookings": 11,
    "cancelledBookings": 1,
    "frequentRoomName": "قاعة الابتكار B"
  },
  "packageSummary": {
    "hasActivePackage": true,
    "packageName": "باقة الطلاب الشهرية - 50 ساعة",
    "remainingHours": 18.5,
    "totalHours": 50.0,
    "expiresAt": "2026-10-01T23:59:59Z"
  },
  "discountEligibility": {
    "qualifyingHours": 182.5,
    "nextMilestoneHours": 200.0,
    "hoursToNextDiscount": 17.5,
    "availableDiscounts": [
      {
        "id": "d1-uuid",
        "name": "خصم التميز - 100 ساعة سابقة (20%)",
        "discountPercentage": 20,
        "isRedeemed": true,
        "redeemedAt": "2026-08-15T12:00:00Z"
      }
    ]
  }
}
```

### Business Logic
- يقوم الباك إند بالاستعلام السريع من جداول جلسات القاعات والورك سبيس المكتملة وتجميع الساعات والزيارات في مهمة واحدة مفهرسة (Indexed Query).

### Authorization / Roles
- مسموح لـ: `SuperAdmin`, `Admin`, `Manager`, `Staff`.

### Priority
⭐ **عالية جداً (P1)**.

---

## فجوة 4: محرك قواعد الخصومات وحماية الاستخدام (Discount Rule Engine & Student Protection)

### Feature
محرك ذكي لاحتساب استحقاق الخصومات تلقائياً للطلاب (بناءً على الساعات، عدد الزيارات، الكلية، الباقة) مع منع إعادة استخدام الخصم قبل تحقيق الهدف التالي.

### Current Backend Support
- `GET /api/Discounts`, `GET /api/Coupons`.
- لا يوجد أي فحص ديناميكي لاستحقاق الطالب، ولا يوجد جدول لتسجيل استهلاك الطالب لخصم معين لمنع تكراره.

### Missing
1. جدول لتعريف قواعد الخصم (`DiscountRules`).
2. جدول لتوثيق استهلاك الطلاب للخصومات (`StudentDiscountRedemptions`).
3. Endpoint لتقييم الخصومات المستحقة للطالب فورياً عند الـ Check-in أو الـ Checkout.

### Required API & Endpoint Proposals
1. `GET /api/Discounts/rules`: جلب قائمة القواعد الفعالة.
2. `POST /api/Discounts/rules`: إنشاء قاعدة خصم جديدة (مثلاً: خصم 25% عند إتمام كل 100 ساعة).
3. `GET /api/Students/{id}/discount-eligibility`: جلب الخصومات المستحقة لهذا الطالب تحديداً.
4. `POST /api/Discounts/redeem`: تسجيل استهلاك خصم مرتبط بجلسة معينة.

### Example Request (`CreateDiscountRuleDto`):
```json
{
  "name": "مكافأة 100 ساعة دراسة",
  "ruleType": "HoursBased", // HoursBased | VisitCountBased | CollegeBased | SpendingBased
  "thresholdValue": 100, // كل 100 ساعة
  "discountPercentage": 20,
  "maxDiscountAmount": 150.00,
  "isRecurringMilestone": true, // يتكرر عند 200, 300, 400
  "applicableTo": "All", // Workspace | Classroom | All
  "isActive": true
}
```

### Business Logic (قاعدة حماية الاستخدام):
- إذا استهلك الطالب الخصم عند وصوله إلى 100 ساعة، يتم تسجيل ذلك في `StudentDiscountRedemptions`.
- النظام يمنع تطبيق الخصم مجدداً لنفس القاعدة إلا بعد أن يتجاوز الطالب حاجز الـ 200 ساعة كاملة، وهكذا.

### Authorization / Roles
- إعداد القواعد: `Admin`, `Manager`.
- الاستعلام والاستهلاك أثناء الحساب: `Staff`.

### Priority
⭐ **عالية ومطلوبة لحوكمة الإيرادات (P2)**.

---

## فجوة 5: إعدادات وحدود التنبيهات التشغيلية (Operational Alerts & Thresholds Settings)

### Feature
واجهة و Endpoints لضبط الحدود التشغيلية للنظام ديناميكياً بدلاً من وضع أرقام ثابتة في الكود (Hardcoded).

### Current Backend Support
- الإعدادات الحالية عامة ولا تشمل حدود التنبيهات.

### Missing
- Endpoints لحفظ وقراءة حدود التنبيه:
  - حد المخزون التحذيري والحرج للكاترينج (مثلاً 10 و 5).
  - حد الإقامة الطويلة (افتراضياً 18 ساعة أو 6 ساعات).
  - حد سعة الغرف المشتركة والصامتة المتبقية (مثلاً <= 5 مقاعد).
  - مدد التنبيه المسبق للجلسات والحجوزات (60 دقيقة و 10 دقائق).

### Required API Proposals
- `GET /api/Settings/operational-alerts`
- `PUT /api/Settings/operational-alerts`

### Request/Response Body:
```json
{
  "cateringWarningThreshold": 10,
  "cateringCriticalThreshold": 5,
  "longStayThresholdHours": 18,
  "roomCapacityAlertSeats": 5,
  "sessionWarningLeadMinutes": 60,
  "sessionUrgentLeadMinutes": 10
}
```

### Priority
🟡 **متوسطة (P2)**.

---

## فجوة 6: بيانات الفواتير الرسمية لجلسات القاعات والورك سبيس (Official Invoicing API)

### Feature
إصدار وطباعة فاتورة رسمية (`Professional PDF / Print Invoice`) عقب إنهاء الجلسة تحتوي على رقم فاتورة تسلسلي موحد وبيانات الوردية والكاشير.

### Current Backend Support
الـ Checkout في القاعات والورك سبيس يرجع كائن الجلسة فقط بدون رقم فاتورة موحد (`invoiceNumber`) أو بصمة مالية ضريبية.

### Missing
- حقل `invoiceNumber` (مثل `INV-202609-00124`) مولد تسلسلياً في الـ Checkout Response.
- أو Endpoint مخصص لإرجاع بيانات الفاتورة الرسمية:
  - `GET /api/Classrooms/{id}/invoice`
  - `GET /api/Workspace/sessions/{id}/invoice`

### Response DTO (`InvoiceDetailDto`):
```json
{
  "invoiceNumber": "INV-2026-09-0412",
  "issueDate": "2026-09-14T15:20:00Z",
  "clientType": "Instructor",
  "clientName": "د. أحمد حسام",
  "clientIdentifier": "INS-104",
  "roomOrDesk": "قاعة المحاضرات الكبرى A",
  "checkInTime": "2026-09-14T10:00:00Z",
  "checkOutTime": "2026-09-14T14:00:00Z",
  "durationFormatted": "4 ساعات",
  "hourlyRate": 60.00,
  "baseAmount": 240.00,
  "cateringAmount": 85.00,
  "printingAmount": 15.00,
  "discountAmount": 40.00,
  "discountReason": "خصم كود الشركاء",
  "subtotal": 340.00,
  "taxAmount": 0.00,
  "finalTotal": 300.00,
  "paymentMethod": "Cash",
  "cashierName": "أحمد مصطفى",
  "shiftId": "2c8e31a1-3321-4d11-b9cd-99114422aa01"
}
```

### Priority
⭐ **عالية جداً للطباعة والتوثيق المالي (P1)**.

---

## فجوة 7: إثراء استجابة قائمة القاعات النشطة لتفادي N+1 Queries

### Feature
عرض أسماء المحاضرين، الغرف، وإجمالي طلبات الكاترينج في قائمة القاعات (`GET /api/Classrooms`).

### Current Backend Support
يرجع الـ `ClassroomDto` الحقول المباشرة فقط:
`id`, `date`, `timeFrom`, `timeTo`, `roomId`, `instructorId`, `reservationCost`, `status`.

### Problem
يضطر الفرونت إند لعمل استدعاء منفصل لكل جلسة لجلب اسمها ورقم المحاضر وطلبات الكاترينج، مما يسبب مشكلة N+1 استدعاءات ويؤخر تحميل الصفحة.

### Required Modification in `ClassroomDto`:
إضافة الحقول المحسوبة التالية إلى استجابة `GET /api/Classrooms`:
- `roomName`: `string`
- `instructorName`: `string`
- `instructorPhoneNumber`: `string`
- `cateringTotal`: `decimal`
- `hourlyRate`: `decimal`
- `totalCost`: `decimal`

### Priority
🔥 **حرجة للأداء والسرعة (P1)**.

---

## مصفوفة الأولويات وخطة التسليم

| الرقم | المتطلب / الميزة | الأولوية | الـ Endpoint المقترح | التأثير على النظام |
|---|---|---|---|---|
| 1 | إثراء بيانات القاعات وتفادي N+1 | P1 (قصوى) | `GET /api/Classrooms` | سرعة تحميل شاشة القاعات والاستغناء عن الطلبات المتعددة |
| 2 | مركز إشعارات الموظفين والإدارة | P1 (قصوى) | `GET/PUT /api/Notifications` + SignalR | تنبيهات لحظية لانتهاء الجلسات ونفاد المخزون وتجاوز الساعات |
| 3 | سجل التدقيق الشامل (Audit Trail) | P1 (قصوى) | `GET /api/AuditLogs` | توثيق كامل لمن قام بكل عملية ومنع التلاعب المالي |
| 4 | بيانات الفواتير الرسمية للطباعة | P1 (قصوى) | `GET /api/Classrooms/{id}/invoice` | طباعة فواتير PDF احترافية متوافقة مع الوردية |
| 5 | تحليلات الطالب المجمعة | P1 (قصوى) | `GET /api/Students/{id}/analytics` | سرعة فتح ملف الطالب وعرض إحصائيات الساعات والزيارات |
| 6 | محرك قواعد الخصومات الذكي | P2 (عالية) | `GET /api/Discounts/rules` | حوكمة الخصومات ومنع تكرار استخدامها بدون استحقاق |
| 7 | إعدادات حدود التنبيهات التشغيلية | P2 (متوسطة)| `GET/PUT /api/Settings/operational-alerts` | مرونة تعديل ساعات الإقامة وحدود المخزون دون إعادة بناء الكود |

---
> **ملاحظة معمارية لفريق الفرونت إند:**  
> تم تصميم وبناء واجهات الفرونت إند الحالية بنظام **Graceful Fallback**؛ بحيث تعمل الميزات الحالية بالاعتماد على الحسابات اللحظية المجمعة من الـ APIs الحالية، وستتحول تلقائياً إلى الـ Endpoints الجديدة فور إطلاقها من الباك إند دون أي تغيير في بنية الـ Components.
